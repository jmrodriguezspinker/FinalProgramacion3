import { BiCategoryAlt } from "react-icons/bi";

const CatalogIcon = ({ size = 28, className = "" }) => {
  return <BiCategoryAlt size={size} color="currentColor" className={className} />;
};

export default CatalogIcon;

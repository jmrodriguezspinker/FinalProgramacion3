// src/components/icons/ProfileIcon.tsx
import { FiUser  } from "react-icons/fi";

const ProfileIcon = ({ size = 22 }) => (
  <FiUser  size={size} color="currentColor" />
);

export default ProfileIcon;
